info:
  name: Get All Monitor Offers
  type: http
  seq: 7

http:
  method: GET
  url: http://localhost:8080/api/monitor-offers
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
